# send_it — Agent Instructions

> Flutter group messaging app (iOS-focused). Generated by TAC OS bootstrap.

## Role

You are an agentic coding agent. Follow TAC methodology:
- **Tactic 1:** Do not type code by hand — use tools
- **Tactic 6:** One agent, one prompt, one purpose — fresh context per step
- **Tactic 7:** Zero-touch engineering — validate, don't hand off broken work

## Stack

- **Framework:** Flutter (FVM — use `fvm flutter`, not bare `flutter`)
- **Language:** Dart
- **UI:** Cupertino (iOS-style)
- **Key packages:** flutter_contacts, url_launcher, permission_handler, shared_preferences, upgrader

## Validation Commands

Run these after every implementation. All must pass before marking work complete.

| Check | Command |
|-------|---------|
| Analyze | `fvm flutter analyze` |
| Test | `fvm flutter test` |
| Build (iOS) | `fvm flutter build ios --no-codesign` (when native changes) |

## Project Structure

```
lib/
├── main.dart              # App entry, HomePage, group list
├── models/                # Data models (MessageGroup)
├── screens/               # UI screens
└── services/              # Storage, contacts, shortcuts
test/                      # Widget/unit tests
specs/                     # Generated plans (do not edit during implement)
app-docs/                  # Feature documentation
ai-docs/                   # Third-party SDK references
```

## Documentation Routing

Read `conditional-docs.md` before planning or implementing.

## Methodology Reference

- Quick ref: `tac-os/knowledge/quick-reference.md`
- Bootstrap guide: `tac-os/BOOTSTRAP.md`
- Engineering rules: `tac-os/rules/engineering-rules.md`

## Workflow

1. `/prime` — understand codebase (fresh agent)
2. `/research` or `/chore` or `/feature` — generate spec/plan in `specs/` (fresh agent)
3. `/implement specs/<plan>.md` — execute plan (fresh agent)

Never reuse the same agent session across planning and implementation.
